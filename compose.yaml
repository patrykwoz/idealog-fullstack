services:
  backend:
    restart: always
    networks:
      - default
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/idealog
    build:
      context: .
      dockerfile: Dockerfile
    ports:  
      - "8000:8000"

networks:
  default:
    driver: bridge